{
  "auths": {
    "{{ addresses.registry }}:5000": {
      "auth": "{{ (docker.registry.credentials.login + ':' + docker.registry.credentials.password) | b64encode }}"
    },
    "docker-registry.service.consul:5000": {
      "auth": "{{ (docker.registry.credentials.login + ':' + docker.registry.credentials.password) | b64encode }}"
    }
  }
}
